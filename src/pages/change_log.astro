---
import "../styles/global.css";
// The rendered page and the repo's CHANGELOG.md are the same file — editing
// the markdown is the only step to publish a change log entry.
import { Content } from "../../CHANGELOG.md";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href={`${import.meta.env.BASE_URL}favicon.svg`} />
    <link rel="icon" href={`${import.meta.env.BASE_URL}favicon.ico`} />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Lato:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <title>Diario — Films with Friends</title>
  </head>
  <body>
    <main class="content">
      <article class="changelog">
        <Content />
      </article>
      <footer class="page-footer">
        <a href={import.meta.env.BASE_URL}>Back to showtimes</a>
      </footer>
    </main>
  </body>
</html>

<style>
  .content {
    max-width: 42rem;
    margin: 0 auto;
    padding: 1rem;
  }

  /* A journal is written in one hand. Nothing here is a headline — the date
     is just the line that starts the entry, and blank space separates one
     day from the next. */
  .changelog :global(h1) {
    font-size: var(--font-size-lg);
    margin-bottom: 0.25rem;
  }

  .changelog :global(h2) {
    font-size: var(--font-size-base);
    margin-top: 2rem;
    margin-bottom: 0.25rem;
  }

  .changelog :global(p) {
    margin: 0.5rem 0;
    line-height: 1.5;
  }

  .page-footer {
    margin: 2.5rem 0 1rem;
    font-size: var(--font-size-sm);
    text-align: center;
  }
</style>
