name: Scrape 4-Star

on:
  schedule:
    - cron: "37 13 * * *" # daily, ~6:37am Pacific (staggered after Balboa/Vogue)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npm run scrape -- four-star

      - name: Commit and push if data changed
        run: |
          if git diff --quiet -- movie-data; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add movie-data
          git commit -m "Update 4-Star showtimes"
          git push
